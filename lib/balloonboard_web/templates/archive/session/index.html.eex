<%= for session <- @sessions do %>
    <a class="btn btn-primary btn-rect" href="<%= Routes.archive_session_path(@conn, :show, session.id) %>" role="button">
        <%= session.id %>
    </a>
<% end %>
