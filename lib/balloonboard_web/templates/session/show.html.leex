<%= live_render(
  @conn,
  BalloonboardWeb.Live.SessionView, session: %{
    session: @session,
    players: @players,
    active_players: @active_players,
    rounds: @rounds,
    tag_href: Routes.session_tags_path(@conn, :tags, @session.id)
}) %>
<footer>Session ID: <%= @session.id %></footer>
