<%= live_render(
  @conn,
  BalloonboardWeb.Live.SessionView, session: %{
    session: @session,
    players: @players,
    active_players: @active_players,
    rounds: @rounds
}) %>
<footer>Session ID: <%= @session.id %></footer>
